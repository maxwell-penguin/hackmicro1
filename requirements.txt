# Direct SDK usage, no LangGraph/LangChain -- the agent loop here is
# linear-with-one-retry-branch and doesn't need graph orchestration.
# Fewer deps = fewer things that can break `make install` on a judge's
# clean environment, which matters for the reproducibility score.
anthropic>=0.40.0
typer>=0.9.0
rich>=13.0.0
pytest>=8.0.0
